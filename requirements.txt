# Core runtime — no torch. Everything here runs on CPU in well under 512MB, which
# is what makes a free-tier instance possible at all.
Flask==3.1.3
gunicorn==23.0.0

# Translation (AWS Bedrock) + HTTP
boto3==1.43.78
requests==2.34.2

# ASR — ctranslate2, not torch
faster-whisper==1.2.1
ctranslate2==4.8.1

# TTS — Piper is ONNX and tiny. Kokoro lives in requirements-full.txt because it
# pulls torch; without it, (hi, male) is served by Piper's pratham voice instead.
piper-tts==1.7.0
onnxruntime==1.23.2

soundfile==0.14.0
numpy==2.2.6
